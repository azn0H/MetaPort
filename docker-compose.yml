services:
  metaport-backend:
    build: ./backend
    container_name: metaport-backend
    restart: unless-stopped
    ports:
      - "8025:8025"
    volumes:
      - ./backend/docs:/app/docs
      - ./backend/.env:/app/.env